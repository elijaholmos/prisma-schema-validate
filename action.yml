name: Prisma Schema Validate
description: GitHub Action to validate a schema.prisma file using the Prisma CLI
author: elijaholmos
branding:
    icon: check
    color: blue
inputs:
    version:
        description: NPM version of Prisma to use
    schema:
        description: Location of the schema.prisma file
runs:
    using: node16
    main: dist/index.js
